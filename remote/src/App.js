import LocalButton from "./Button";
import ThemeWrapper from "./ThemeWrapper";

const App = () => (
  <div>
    <h1>Theme UI Remote</h1>
    <ThemeWrapper>
      <LocalButton>Local button</LocalButton>
    </ThemeWrapper>
  </div>
);

export default App;
